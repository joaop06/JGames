# Database (used by backend and docker-compose)
POSTGRES_USER=games
POSTGRES_DB=games_platform
POSTGRES_PASSWORD=games_secret

# Backend - build from backend/ with: DATABASE_URL and JWT_SECRET
# DATABASE_URL is set by docker-compose from POSTGRES_* for backend service
JWT_SECRET=change-me-in-production-use-long-random-string
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Única porta exposta no host: o frontend faz proxy de /api e /health para o backend
FRONTEND_PORT=5173

# Frontend (optional - for local dev without Docker)
# VITE_API_URL=http://localhost:3000
